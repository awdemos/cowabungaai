apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "turso.serviceAccountName" . }}
  labels:
    {{- include "turso.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
