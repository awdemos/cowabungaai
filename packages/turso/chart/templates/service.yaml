apiVersion: v1
kind: Service
metadata:
  name: {{ include "turso.fullname" . }}
  labels:
    {{- include "turso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "turso.selectorLabels" . | nindent 4 }}
